import type { PilotOutcomeGuardrails, StrategicPilotOutcomeScenario } from '@/types/sensetrust/strategic-pilot-outcome-closeout-decision-room'

export const STRATEGIC_PILOT_OUTCOME_REQUIRED_STATEMENT = 'A SenseTrust v4.0 não certifica resultado clínico, científico, regulatório ou comercial. Ela consolida um fechamento simulado, auditável e supervisionado de piloto institucional, organizando evidências, riscos, desvios, aprendizados e decisão humana Go/Pause/Refine/Stop.'
export const STRATEGIC_PILOT_OUTCOME_REFERENCES = ['v3.9 Monitoring & Evidence Intake Room', 'v3.8 Pilot Execution Governance Room', 'v3.7 Pilot Proposal Room', 'SenseTrust Git Freeze Automation v1.2 Lean Mode', 'v4.1 institutional handoff preparation', 'DNDA = Diagnóstico Neurofuncional Dimensional Auditável']
export const STRATEGIC_PILOT_OUTCOME_GUARDRAILS: PilotOutcomeGuardrails = { simulated_only: true, metadata_only: true, human_review_required: true, handoff_preparation_only: true, contains_clinical_data: false, contains_patient_data: false, contains_personal_sensitive_data: false, real_clinical_operation_claim: false, real_pilot_execution_claim: false, real_outcome_claim: false, real_biological_sample_claim: false, real_eeg_claim: false, real_qeeg_claim: false, real_biomarker_claim: false, real_client_claim: false, real_contract_claim: false, real_partnership_claim: false, real_billing_claim: false, real_revenue_claim: false, real_crm_claim: false, real_email_automation_claim: false, regulatory_authorization_claim: false, scientific_validation_claim: false, external_certification_claim: false, diagnostic_truth_certification_claim: false, treatment_claim: false, outcome_guarantee_claim: false, public_health_claim: false }

function scenario(roomId: string, simulatedOrganizationName: string, finalDecision: StrategicPilotOutcomeScenario['closeoutDecision'], score: number): StrategicPilotOutcomeScenario {
  const base = { ...STRATEGIC_PILOT_OUTCOME_GUARDRAILS, roomId, simulatedOrganizationName, linkedV39MonitoringRoomId: `v39-${roomId}`, linkedV38ExecutionGovernanceRoomId: `v38-${roomId}`, linkedV37ProposalRoomId: `v37-${roomId}`, linkedV12LeanFreezeReference: 'SenseTrust Git Freeze Automation v1.2 Lean Mode', closeoutStage: finalDecision === 'handoff_ready' ? 'handoff_ready' : finalDecision === 'go' ? 'go_recommended' : finalDecision === 'pause' ? 'pause_recommended' : finalDecision === 'refine' ? 'refine_recommended' : 'human_review_required', closeoutDecision: finalDecision, outcomeReadinessScore: score, evidenceSynthesisScore: score - 3, riskResolutionScore: score - 6, learningLoopScore: score - 2, handoffReadinessScore: score - 4, v41InstitutionalHandoffPreparation: true } as const
  const outcomeSummaries = [{ id: `${roomId}-summary`, roomId, summary: `${simulatedOrganizationName} consolidado em closeout simulado metadata_only, sem outcome real e com decisão humana supervisionada.`, confidenceLevel: score > 80 ? 'high' : 'moderate', simulated_only: true, metadata_only: true, human_review_required: true }] as const
  const evidenceItems = ['evidências simuladas de governança', 'checkpoints simulados', 'riscos e desvios simulados'].map((title, index) => ({ id: `${roomId}-evidence-${index}`, title, status: index === 2 ? 'partial' : 'ready', confidenceLevel: index === 2 ? 'moderate' : 'high', simulated_only: true, metadata_only: true }) as const)
  const lessons = ['Separar simulação de operação real', 'Manter decisão humana antes de handoff', 'Registrar limites de BLC e Trust Layer'].map((lesson, index) => ({ id: `${roomId}-lesson-${index}`, lesson, nextAction: 'preparar v4.1 institutional handoff sem executar piloto real', simulated_only: true, metadata_only: true }) as const)
  const decisionItems = ['evidência suficiente', 'risco residual aceitável', 'aprendizado consolidado'].map((criterion, index) => ({ id: `${roomId}-decision-${index}`, criterion, decision: index === 1 && score < 80 ? 'refine' : finalDecision, confidenceLevel: index === 1 ? 'moderate' : 'high', simulated_only: true, metadata_only: true }) as const)
  return { ...base, outcomeSummaries: [...outcomeSummaries], evidenceSynthesis: { id: `${roomId}-synthesis`, roomId, items: [...evidenceItems], synthesis: 'Síntese simulada de evidências: suficiente para closeout auditável, não para certificação clínica, científica, regulatória ou comercial.', simulated_only: true, metadata_only: true, human_review_required: true }, checkpointReviews: [{ id: `${roomId}-checkpoint`, roomId, checkpoint: 'closeout criteria', status: score > 78 ? 'ready' : 'partial', simulated_only: true, metadata_only: true, human_review_required: true }], riskReviews: [{ id: `${roomId}-risk`, roomId, risk: 'confusão entre simulação e operação real', status: 'requires_review', mitigation: 'boundary claims guardrail e revisão humana obrigatória', simulated_only: true, metadata_only: true, human_review_required: true }], deviationReviews: [{ id: `${roomId}-deviation`, roomId, deviation: 'lacuna simulada de maturidade institucional', closeoutAction: 'registrar aprendizado e encaminhar handoff apenas se aprovado por humano', simulated_only: true, metadata_only: true, human_review_required: true }], learningLoop: { id: `${roomId}-learning`, roomId, lessons: [...lessons], simulated_only: true, metadata_only: true, human_review_required: true }, decisionBoard: { id: `${roomId}-board`, roomId, items: [...decisionItems], finalDecision, simulated_only: true, metadata_only: true, human_review_required: true }, readinessScorecard: { id: `${roomId}-scorecard`, roomId, outcomeReadinessScore: score, evidenceSynthesisScore: score - 3, riskResolutionScore: score - 6, learningLoopScore: score - 2, handoffReadinessScore: score - 4, status: score > 82 ? 'ready' : 'partial', simulated_only: true, metadata_only: true, human_review_required: true }, institutionalMaturityMatrix: { id: `${roomId}-maturity`, roomId, maturityNotes: ['maturidade institucional simulada', 'governança BLC rastreável', 'sem certificação diagnóstica'], status: score > 82 ? 'ready' : 'requires_review', simulated_only: true, metadata_only: true, human_review_required: true }, handoffReadinessBoard: { id: `${roomId}-handoff`, roomId, handoffDecision: finalDecision === 'go' ? 'handoff_ready' : finalDecision, blockers: score > 82 ? [] : ['revisão humana antes de v4.1'], simulated_only: true, metadata_only: true, human_review_required: true, v41InstitutionalHandoffPreparation: true }, humanReviewQueue: { id: `${roomId}-human`, roomId, reasons: ['validar Go/Pause/Refine/Stop', 'impedir claim de outcome real'], simulated_only: true, metadata_only: true, human_review_required: true }, boundaryClaimsGuardrails: [{ id: `${roomId}-guardrail`, roomId, claimBoundary: 'no real outcome, no real pilot, no diagnostic truth certification, no client, no contract, no partnership', status: 'compliant', simulated_only: true, metadata_only: true, human_review_required: true }], auditTrail: { id: `${roomId}-audit`, roomId, events: ['v3.7 proposal simulated', 'v3.8 governance simulated', 'v3.9 intake simulated', 'v4.0 closeout simulated'], append_only_simulated: true, simulated_only: true, metadata_only: true }, executiveReport: { id: `${roomId}-report`, roomId, summary: STRATEGIC_PILOT_OUTCOME_REQUIRED_STATEMENT, finalDecision, v41InstitutionalHandoffPreparation: true, simulated_only: true, metadata_only: true, human_review_required: true } }
}

export const SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS = [scenario('prefeitura-closeout-simulado', 'Prefeitura simulada / closeout documental institucional', 'handoff_ready', 86), scenario('clinica-dnda-closeout-simulado', 'Clínica simulada / closeout DNDA metadata_only', 'refine', 78), scenario('biostrata-agro-closeout-simulado', 'Parceiro BioStrata Agro simulado / closeout de cadeia de evidência operacional não clínica', 'go', 84)]
export const SIMULATED_STRATEGIC_PILOT_OUTCOME_CLOSEOUT_ROOMS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map(({ outcomeSummaries, evidenceSynthesis, checkpointReviews, riskReviews, deviationReviews, learningLoop, decisionBoard, readinessScorecard, institutionalMaturityMatrix, handoffReadinessBoard, humanReviewQueue, boundaryClaimsGuardrails, auditTrail, executiveReport, ...room }) => room)
export const SIMULATED_PILOT_OUTCOME_SUMMARIES = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.flatMap((x) => x.outcomeSummaries)
export const SIMULATED_PILOT_OUTCOME_EVIDENCE_SYNTHESIS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.evidenceSynthesis)
export const SIMULATED_PILOT_OUTCOME_EVIDENCE_ITEMS = SIMULATED_PILOT_OUTCOME_EVIDENCE_SYNTHESIS.flatMap((x) => x.items)
export const SIMULATED_PILOT_OUTCOME_CHECKPOINT_REVIEWS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.flatMap((x) => x.checkpointReviews)
export const SIMULATED_PILOT_OUTCOME_RISK_REVIEWS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.flatMap((x) => x.riskReviews)
export const SIMULATED_PILOT_OUTCOME_DEVIATION_REVIEWS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.flatMap((x) => x.deviationReviews)
export const SIMULATED_PILOT_OUTCOME_LEARNING_LOOPS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.learningLoop)
export const SIMULATED_PILOT_OUTCOME_LESSONS_LEARNED = SIMULATED_PILOT_OUTCOME_LEARNING_LOOPS.flatMap((x) => x.lessons)
export const SIMULATED_PILOT_OUTCOME_DECISION_BOARDS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.decisionBoard)
export const SIMULATED_PILOT_OUTCOME_DECISION_ITEMS = SIMULATED_PILOT_OUTCOME_DECISION_BOARDS.flatMap((x) => x.items)
export const SIMULATED_PILOT_OUTCOME_READINESS_SCORECARDS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.readinessScorecard)
export const SIMULATED_PILOT_OUTCOME_INSTITUTIONAL_MATURITY_MATRICES = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.institutionalMaturityMatrix)
export const SIMULATED_PILOT_OUTCOME_HANDOFF_READINESS_BOARDS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.handoffReadinessBoard)
export const SIMULATED_PILOT_OUTCOME_HUMAN_REVIEW_QUEUES = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.humanReviewQueue)
export const SIMULATED_PILOT_OUTCOME_BOUNDARY_CLAIMS_GUARDRAILS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.flatMap((x) => x.boundaryClaimsGuardrails)
export const SIMULATED_PILOT_OUTCOME_AUDIT_TRAILS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.auditTrail)
export const SIMULATED_STRATEGIC_PILOT_OUTCOME_EXECUTIVE_REPORTS = SIMULATED_STRATEGIC_PILOT_OUTCOME_SCENARIOS.map((x) => x.executiveReport)
