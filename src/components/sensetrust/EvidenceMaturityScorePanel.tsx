import type { SenseTrustEvidenceMaturityScore } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function EvidenceMaturityScorePanel({ scores }: { scores: SenseTrustEvidenceMaturityScore[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Evidence Maturity Score</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{scores.slice(0, 8).map((x) => <div key={x.maturity_score_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xl font-black text-slate-950">{x.aggregate_score}%</p><p className="text-xs text-slate-600">nao certificacao externa</p></div>)}</div></section> }
