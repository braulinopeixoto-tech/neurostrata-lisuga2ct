import type { SenseTrustPartnerEngagementTrack } from '@/types/sensetrust/strategic-partner-engagement-control-room'
export function PartnerEngagementTrackPanel({ tracks }: { tracks: SenseTrustPartnerEngagementTrack[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Partner Engagement Tracks</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{tracks.slice(0, 8).map((track) => <div key={track.partner_engagement_track_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{track.partner_label}</p><p className="text-xs text-slate-600">{track.engagement_stage} / {track.recommended_decision}</p></div>)}</div></section> }
