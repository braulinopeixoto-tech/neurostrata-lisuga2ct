import type { PilotExecutionGovernanceState } from '@/types/sensetrust/strategic-pilot-execution-governance-room'
const badges = ['simulated_only', 'metadata_only', 'human_review_required', 'no_real_pilot', 'no_contract', 'no_client', 'no_diagnostic_certification']
export function StrategicPilotExecutionGovernanceRoomDashboard({ state }: { state: PilotExecutionGovernanceState }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xl font-black text-slate-950">SenseTrust Strategic Pilot Execution Governance Room v3.8</p><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">simulated execution governance / checkpoints / evidence / human decision</p></div><span className="rounded-md bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">no real pilot</span></div><div className="mt-4 grid gap-3 sm:grid-cols-6"><Metric label="rooms" value={state.rooms.length} /><Metric label="scenarios" value={state.scenarios.length} /><Metric label="gates" value={state.activationGates.length} /><Metric label="checkpoints" value={state.checkpoints.length} /><Metric label="evidence" value={state.evidenceRecords.length} /><Metric label="reviews" value={state.humanReviewItems.length} /></div><div className="mt-4 flex flex-wrap gap-2">{badges.map((x) => <span key={x} className="rounded bg-slate-100 px-2 py-1 text-xs font-bold uppercase text-slate-700">{x}</span>)}</div></section> }
function Metric({ label, value }: { label: string; value: number }) { return <div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-1 font-mono text-xl font-black text-slate-950">{value}</p></div> }
