import type { SenseTrustConversionQualificationMatrix } from '@/types/sensetrust/strategic-partnership-conversion-decision-room'
export function ConversionQualificationMatrixPanel({ matrices }: { matrices: SenseTrustConversionQualificationMatrix[] }) { return <Panel title="Conversion Qualification Matrix" items={matrices.map((x) => [x.qualification_matrix_id, `${x.items.length} criterios simulados`])} /> }
function Panel({ title, items }: { title: string; items: string[][] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">{title}</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{items.map(([k, v]) => <div key={k} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{v}</p><p className="text-xs text-slate-600">metadata_only</p></div>)}</div></section> }
