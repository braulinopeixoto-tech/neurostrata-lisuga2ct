import type { SenseTrustLearningLoopItem } from '@/types/sensetrust/institutional-pilot-closeout'
export function LearningLoopRegisterPanel({ items }: { items: SenseTrustLearningLoopItem[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Learning loop register</p><div className="mt-4 grid gap-3 md:grid-cols-3">{items.slice(0, 15).map((item) => <div key={item.learning_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xs font-bold uppercase text-rose-700">{item.priority} / {item.learning_category}</p><p className="mt-2 text-sm font-black text-slate-900">{item.learning_title}</p><p className="mt-1 text-xs text-slate-600">{item.learning_description}</p><p className="mt-1 text-xs text-slate-600">Action: {item.action_required}</p><p className="mt-1 text-xs font-bold text-slate-700">Owner/v3: {item.owner_role} / {item.linked_v3_requirement}</p></div>)}</div></section> }
