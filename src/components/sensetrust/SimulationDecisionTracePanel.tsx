import type { SenseTrustSimulationDecisionTrace } from '@/types/sensetrust/strategic-scale-simulation-console'
export function SimulationDecisionTracePanel({ traces }: { traces: SenseTrustSimulationDecisionTrace[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Simulation Decision Trace</h2><div className="mt-3 space-y-2">{traces.slice(0, 4).map((item) => <div key={item.trace_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{item.trace_id}</p><p className="text-xs text-slate-600">{item.trace_items.length} eventos metadata_only</p></div>)}</div></section> }
