import type { SenseTrustEvidenceToLearningMap } from '@/types/sensetrust/institutional-pilot-closeout'
export function EvidenceToLearningMapPanel({ maps }: { maps: SenseTrustEvidenceToLearningMap[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Evidence-to-learning map</p><div className="mt-4 grid gap-3 md:grid-cols-2">{maps.map((item) => <div key={item.map_id} className="rounded-md bg-slate-50 p-3"><p className="text-sm font-black text-slate-900">{item.evidence_id} to {item.learning_id}</p><p className="mt-1 text-xs text-slate-600">Decision/v3: {item.decision_id} / {item.v3_requirement}</p><p className="mt-1 text-xs text-slate-600">{item.risk_mitigated}</p></div>)}</div></section> }
