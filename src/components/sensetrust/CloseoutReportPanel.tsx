import type { SenseTrustInstitutionalPilotCloseoutReport } from '@/types/sensetrust/institutional-pilot-closeout'
export function CloseoutReportPanel({ reports }: { reports: SenseTrustInstitutionalPilotCloseoutReport[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Closeout reports</p><div className="mt-4 grid gap-3 md:grid-cols-2">{reports.map((item) => <div key={item.closeout_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xs font-bold text-slate-500">{item.closeout_status} / {item.logical_closeout_hash}</p><p className="mt-2 text-sm font-black text-slate-900">{item.closeout_title}</p><p className="mt-1 text-xs text-slate-600">Pilot/cert/vault/ledger: {item.pilot_id} / {item.certificate_id} / {item.evidence_vault_id} / {item.acceptance_ledger_id}</p><p className="mt-1 text-xs text-slate-600">{item.outcome_summary.summary}</p></div>)}</div></section> }
