import type { PartnerPresentationExecutiveReport } from '@/types/sensetrust/demo-distribution-partner-presentation-kit'
export function PartnerPresentationExecutiveReportPanel({ reports }: { reports: PartnerPresentationExecutiveReport[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Executive report</h2>{reports.map((x) => <article key={x.id} className="mt-2 rounded bg-slate-50 p-3 text-sm"><p className="font-black text-emerald-700">{x.decision}</p><p>{x.summary}</p><p className="mt-2 font-bold">demo_distribution_only partner_presentation_only release_candidate_linked</p></article>)}</section> }
