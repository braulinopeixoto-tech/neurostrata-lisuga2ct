import type { PilotMonitoringAuditTrail } from '@/types/sensetrust/strategic-pilot-monitoring-evidence-intake-room'
export function PilotMonitoringAuditTrailPanel({ trails }: { trails: PilotMonitoringAuditTrail[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Monitoring Audit Trail</h2><div className="mt-3 grid gap-2 md:grid-cols-3">{trails.map((x) => <div key={x.id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{x.id}</p><p className="text-xs text-slate-600">append-only simulated / {x.events.length} events</p></div>)}</div></section> }
