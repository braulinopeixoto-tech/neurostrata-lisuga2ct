import type { SenseTrustInstitutionalHandoffRoom } from '@/types/sensetrust/institutional-handoff-room'
export function SenseTrustInstitutionalHandoffRoomPanel({ rooms }: { rooms: SenseTrustInstitutionalHandoffRoom[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black text-slate-950">Institutional handoff rooms</h2><div className="mt-3 grid gap-3 lg:grid-cols-4">{rooms.map((room) => <article key={room.roomId} className="rounded-md border p-3"><p className="font-bold text-slate-900">{room.simulatedOrganizationName}</p><p className="text-sm text-slate-600">{room.handoffStage} / {room.handoffDecision}</p><p className="mt-2 font-mono text-sm">score {room.handoffReadinessScore}</p><p className="mt-2 text-xs font-bold text-emerald-700">simulated_only metadata_only human_review_required</p></article>)}</div></section> }
