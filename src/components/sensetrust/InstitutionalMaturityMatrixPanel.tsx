import type { SenseTrustInstitutionalMaturityMatrix } from '@/types/sensetrust/institutional-pilot-closeout'
export function InstitutionalMaturityMatrixPanel({ matrices }: { matrices: SenseTrustInstitutionalMaturityMatrix[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Institutional maturity matrix</p><div className="mt-4 grid gap-3 md:grid-cols-4">{matrices.map((item) => <div key={item.matrix_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xl font-black text-slate-950">{item.score}%</p><p className="text-xs font-bold text-slate-700">{item.maturity_level}</p><p className="mt-1 text-xs text-slate-600">{item.domain}</p><p className="mt-1 text-xs text-rose-700">Gaps: {item.gaps.join(', ')}</p><p className="mt-1 text-xs text-slate-600">{item.recommendation}</p></div>)}</div></section> }
