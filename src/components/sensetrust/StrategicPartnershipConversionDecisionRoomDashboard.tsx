import type { SenseTrustStrategicPartnershipConversionDecisionRoomState } from '@/types/sensetrust/strategic-partnership-conversion-decision-room'
export function StrategicPartnershipConversionDecisionRoomDashboard({ state }: { state: SenseTrustStrategicPartnershipConversionDecisionRoomState }) { const avg = Math.round(state.readiness_scorecards.reduce((s, x) => s + x.aggregate_score, 0) / state.readiness_scorecards.length); const guardrails = ['metadata_only', 'no contract', 'no client', 'no partnership', 'no binding proposal', 'no legal commitment', 'no CRM', 'no real email']; return <section className="rounded-md border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xl font-black text-slate-950">SenseTrust Strategic Partnership Conversion Decision Room v3.6</p><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">qualification / non-binding intent / diligence / review routing / decision</p></div><span className="rounded-md bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">no binding proposal</span></div><div className="mt-4 grid gap-3 sm:grid-cols-6"><Metric label="rooms" value={state.rooms.length} /><Metric label="candidates" value={state.candidates.length} /><Metric label="intent" value={state.intent_items.length} /><Metric label="reviews" value={state.legal_review_items.length + state.scientific_review_items.length + state.regulatory_review_items.length} /><Metric label="guardrails" value={state.boundary_claims_guardrail_items.length} /><Metric label="readiness" value={avg} suffix="%" /></div><div className="mt-4 flex flex-wrap gap-2">{guardrails.map((item) => <span key={item} className="rounded bg-slate-100 px-2 py-1 text-xs font-bold uppercase text-slate-700">{item}</span>)}</div></section> }
function Metric({ label, value, suffix = '' }: { label: string; value: number; suffix?: string }) { return <div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-1 font-mono text-xl font-black text-slate-950">{value}{suffix}</p></div> }
