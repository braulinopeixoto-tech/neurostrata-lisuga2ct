import type { SenseTrustCloseoutDecision } from '@/types/sensetrust/institutional-pilot-closeout'
export function CloseoutDecisionBoardPanel({ decisions }: { decisions: SenseTrustCloseoutDecision[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Go / Pause / Refine decision board</p><div className="mt-4 grid gap-3 md:grid-cols-2">{decisions.slice(0, 12).map((item) => <div key={item.decision_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xs font-bold text-slate-500">{item.timestamp_simulated} / {item.decision_hash}</p><p className="mt-2 text-sm font-black text-slate-900">{item.decision_type}</p><p className="mt-1 text-xs text-slate-600">{item.decision_reason}</p><p className="mt-1 text-xs text-slate-600">Reviews: {item.required_reviews.join(', ')}</p><p className="mt-1 text-xs text-rose-700">Blocked: {item.blocked_actions.join(', ')}</p><p className="mt-1 text-xs font-bold text-slate-700">{item.next_action}</p></div>)}</div></section> }
