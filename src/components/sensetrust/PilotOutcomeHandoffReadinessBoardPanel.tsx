import type { PilotOutcomeHandoffReadinessBoard } from '@/types/sensetrust/strategic-pilot-outcome-closeout-decision-room'
export function PilotOutcomeHandoffReadinessBoardPanel({ boards }: { boards: PilotOutcomeHandoffReadinessBoard[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Handoff readiness board</h2><div className="mt-2 grid gap-2 lg:grid-cols-3">{boards.map((x) => <article key={x.id} className="rounded bg-slate-50 p-2 text-sm"><p className="font-black">{x.handoffDecision}</p><p>blockers: {x.blockers.length ? x.blockers.join(', ') : 'none simulated'}</p><p className="font-bold text-emerald-700">v4.1 institutional handoff preparation</p></article>)}</div></section> }
