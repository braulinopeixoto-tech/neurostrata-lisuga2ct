import type { SenseTrustMinimumEvidenceMatrix } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function MinimumEvidenceMatrixPanel({ matrices }: { matrices: SenseTrustMinimumEvidenceMatrix[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Minimum Evidence Matrix</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{matrices.slice(0, 8).map((x) => <div key={x.matrix_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-lg font-black text-slate-950">{x.requirements.length}</p><p className="text-xs text-slate-600">requisitos simulados</p></div>)}</div></section> }
