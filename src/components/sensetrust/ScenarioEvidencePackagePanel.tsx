import type { SenseTrustScenarioEvidencePackage } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function ScenarioEvidencePackagePanel({ packages }: { packages: SenseTrustScenarioEvidencePackage[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Scenario Evidence Packages</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{packages.slice(0, 8).map((x) => <div key={x.evidence_package_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{x.evidence_strength_level}</p><p className="text-xs text-slate-600">{x.evidence_items.length} itens; nao prova clinica</p></div>)}</div></section> }
