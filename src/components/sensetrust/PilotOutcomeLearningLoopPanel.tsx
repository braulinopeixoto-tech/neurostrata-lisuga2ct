import type { PilotOutcomeLearningLoop } from '@/types/sensetrust/strategic-pilot-outcome-closeout-decision-room'
export function PilotOutcomeLearningLoopPanel({ loops }: { loops: PilotOutcomeLearningLoop[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Learning loop</h2><div className="mt-2 grid gap-2 lg:grid-cols-3">{loops.map((x) => <article key={x.id} className="rounded bg-slate-50 p-2 text-sm"><p className="font-bold">{x.roomId}</p>{x.lessons.map((lesson) => <p key={lesson.id}>{lesson.lesson}</p>)}</article>)}</div></section> }
