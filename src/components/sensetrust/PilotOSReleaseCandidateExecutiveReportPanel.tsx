import type { PilotOSReleaseCandidateExecutiveReport } from '@/types/sensetrust/pilot-os-release-candidate'
export function PilotOSReleaseCandidateExecutiveReportPanel({ reports }: { reports: PilotOSReleaseCandidateExecutiveReport[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Executive report</h2>{reports.map((x) => <article key={x.id} className="mt-2 rounded bg-slate-50 p-3 text-sm"><p className="font-black text-emerald-700">{x.decision}</p><p>{x.summary}</p><p className="mt-2 font-bold">release_candidate_only command_center_linked</p></article>)}</section> }
