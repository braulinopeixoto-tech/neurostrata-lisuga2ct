import type { SenseTrustReadinessScoreSimulation } from '@/types/sensetrust/strategic-scale-simulation-console'
export function ReadinessScoreSimulationPanel({ simulations }: { simulations: SenseTrustReadinessScoreSimulation[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Readiness Score Simulation</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{simulations.slice(0, 8).map((item) => <div key={item.simulation_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xl font-black text-slate-950">{item.aggregate_score}%</p><p className="text-xs text-slate-600">sem validacao comercial/regulatoria</p></div>)}</div></section> }
