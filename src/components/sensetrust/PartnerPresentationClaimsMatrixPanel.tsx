import type { PartnerPresentationAuthorizedClaim, PartnerPresentationProhibitedClaim } from '@/types/sensetrust/demo-distribution-partner-presentation-kit'
export function PartnerPresentationClaimsMatrixPanel({ authorized, prohibited }: { authorized: PartnerPresentationAuthorizedClaim[]; prohibited: PartnerPresentationProhibitedClaim[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Claims matrix</h2><div className="mt-2 grid gap-2 lg:grid-cols-2"><div>{authorized.map((x) => <p key={x.id} className="rounded bg-emerald-50 p-2 text-sm">{x.status}: {x.claim}</p>)}</div><div>{prohibited.map((x) => <p key={x.id} className="rounded bg-rose-50 p-2 text-sm">{x.status}: {x.claim}</p>)}</div></div></section> }
