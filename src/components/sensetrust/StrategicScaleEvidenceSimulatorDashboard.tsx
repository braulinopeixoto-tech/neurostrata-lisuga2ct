import type { SenseTrustStrategicScaleEvidenceSimulatorState } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function StrategicScaleEvidenceSimulatorDashboard({ state }: { state: SenseTrustStrategicScaleEvidenceSimulatorState }) { const maturity = Math.round(state.maturity_scores.reduce((s, x) => s + x.aggregate_score, 0) / state.maturity_scores.length); const guardrails = ['metadata_only', 'no clinical data', 'no patient data', 'no contract', 'no client', 'no partnership', 'no regulatory authorization', 'no diagnostic truth certification', 'no real scientific validation', 'no external certification']; return <section className="rounded-md border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xl font-black text-slate-950">SenseTrust Strategic Scale Evidence Simulator v3.3</p><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">evidence packages / gaps / proof risk / maturity / human review</p></div><span className="rounded-md bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">no scientific validation</span></div><div className="mt-4 grid gap-3 sm:grid-cols-6"><Metric label="simulators" value={state.evidence_simulators.length} /><Metric label="packages" value={state.evidence_packages.length} /><Metric label="evidence" value={state.evidence_items.length} /><Metric label="gaps" value={state.gap_items.length} /><Metric label="risk" value={state.proof_risk_items.length} /><Metric label="maturity" value={maturity} suffix="%" /></div><div className="mt-4 flex flex-wrap gap-2">{guardrails.map((item) => <span key={item} className="rounded bg-slate-100 px-2 py-1 text-xs font-bold uppercase text-slate-700">{item}</span>)}</div></section> }
function Metric({ label, value, suffix = '' }: { label: string; value: number; suffix?: string }) { return <div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-1 font-mono text-xl font-black text-slate-950">{value}{suffix}</p></div> }
