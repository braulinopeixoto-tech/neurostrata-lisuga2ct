import type { CommandCenterExecutiveReport } from '@/types/sensetrust/command-center-integration'
export function CommandCenterExecutiveReportPanel({ reports }: { reports: CommandCenterExecutiveReport[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Executive report</h2><div className="mt-2 grid gap-2">{reports.map((x) => <article key={x.id} className="rounded bg-slate-50 p-3 text-sm"><p className="font-black text-emerald-700">{x.decision}</p><p>{x.summary}</p><p className="mt-2 font-bold">command_center_only demo_preparation_only</p></article>)}</div></section> }
