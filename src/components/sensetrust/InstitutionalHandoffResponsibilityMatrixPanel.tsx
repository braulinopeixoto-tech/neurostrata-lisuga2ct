import type { InstitutionalHandoffResponsibilityMatrix } from '@/types/sensetrust/institutional-handoff-room'
export function InstitutionalHandoffResponsibilityMatrixPanel({ matrices }: { matrices: InstitutionalHandoffResponsibilityMatrix[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Responsibility matrix</h2><div className="mt-2 grid gap-2 lg:grid-cols-4">{matrices.map((x) => <article key={x.id} className="rounded bg-slate-50 p-2 text-sm"><p className="font-bold">{x.roomId}</p>{x.responsibilities.map((item) => <p key={item}>{item}</p>)}</article>)}</div></section> }
