import type { SenseTrustEvidenceReadinessBoard } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function EvidenceReadinessBoardPanel({ boards }: { boards: SenseTrustEvidenceReadinessBoard[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Evidence Readiness Board</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{boards.slice(0, 8).map((x) => <div key={x.board_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{x.recommended_decision}</p><p className="text-xs text-slate-600">{x.items.length} decisoes simuladas</p></div>)}</div></section> }
