import type { PilotExecutionActivationGate } from '@/types/sensetrust/strategic-pilot-execution-governance-room'
export function PilotExecutionActivationGatePanel({ gates }: { gates: PilotExecutionActivationGate[] }) { return <SimplePanel title="Activation Gates" items={gates.map((x) => ({ id: x.id, title: x.title, detail: `${x.status} / ${x.requiredDomain}` }))} /> }
function SimplePanel({ title, items }: { title: string; items: { id: string; title: string; detail: string }[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">{title}</h2><div className="mt-3 grid gap-2 md:grid-cols-3">{items.slice(0, 9).map((x) => <div key={x.id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{x.title}</p><p className="text-xs text-slate-600">{x.detail}</p></div>)}</div></section> }
