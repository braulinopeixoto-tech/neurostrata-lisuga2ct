import type { SenseTrustPilotOSReleaseCandidate } from '@/types/sensetrust/pilot-os-release-candidate'
export function SenseTrustPilotOSReleaseCandidatePanel({ candidates }: { candidates: SenseTrustPilotOSReleaseCandidate[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Release candidates</h2>{candidates.map((x) => <article key={x.releaseCandidateId} className="mt-2 rounded bg-slate-50 p-3 text-sm"><p className="font-bold">{x.title}</p><p>{x.stage} / {x.decision}</p><p>Command Center: {x.linkedV42CommandCenterId}</p></article>)}</section> }
