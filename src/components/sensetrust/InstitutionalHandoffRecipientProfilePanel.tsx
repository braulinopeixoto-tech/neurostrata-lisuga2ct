import type { InstitutionalHandoffRecipientProfile } from '@/types/sensetrust/institutional-handoff-room'
export function InstitutionalHandoffRecipientProfilePanel({ profiles }: { profiles: InstitutionalHandoffRecipientProfile[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Recipient profiles</h2><div className="mt-2 grid gap-2 lg:grid-cols-4">{profiles.map((x) => <article key={x.id} className="rounded bg-slate-50 p-2 text-sm"><p className="font-bold">{x.recipientType}</p><p>{x.label}</p><p>{x.reviewNeed}</p></article>)}</div></section> }
