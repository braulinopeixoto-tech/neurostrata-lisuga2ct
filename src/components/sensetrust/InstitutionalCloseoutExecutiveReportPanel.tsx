import type { SenseTrustInstitutionalCloseoutExecutiveReport } from '@/types/sensetrust/institutional-pilot-closeout'
export function InstitutionalCloseoutExecutiveReportPanel({ reports }: { reports: SenseTrustInstitutionalCloseoutExecutiveReport[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Institutional closeout executive report</p><div className="mt-4 grid gap-3 md:grid-cols-2">{reports.map((report) => <div key={report.report_id} className="rounded-md bg-slate-50 p-3"><p className="text-sm font-black text-slate-900">{report.executive_summary}</p><p className="mt-1 text-xs text-slate-600">Decision: {report.recommended_decision}</p><p className="mt-1 text-xs text-slate-600">Maturity: {report.maturity_summary}</p><p className="mt-1 text-xs text-slate-600">v3: {report.v3_readiness_summary}</p><p className="mt-1 text-xs text-rose-700">Risks: {report.risk_summary}</p><p className="mt-2 text-xs font-black text-slate-800">{report.recommendation}</p></div>)}</div></section> }
