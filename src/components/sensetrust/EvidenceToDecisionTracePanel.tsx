import type { SenseTrustEvidenceToDecisionTrace } from '@/types/sensetrust/strategic-scale-evidence-simulator'
export function EvidenceToDecisionTracePanel({ traces }: { traces: SenseTrustEvidenceToDecisionTrace[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Evidence-to-Decision Trace</h2><div className="mt-3 space-y-2">{traces.slice(0, 4).map((x) => <div key={x.trace_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{x.trace_id}</p><p className="text-xs text-slate-600">{x.trace_items.length} eventos metadata_only</p></div>)}</div></section> }
