import type { SenseTrustRegulatoryPendingItem } from '@/types/sensetrust/institutional-pilot-closeout'
export function RegulatoryPendingItemsPanel({ items }: { items: SenseTrustRegulatoryPendingItem[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Regulatory pending items</p><div className="mt-4 grid gap-3 md:grid-cols-3">{items.slice(0, 12).map((item) => <div key={item.pending_id} className="rounded-md bg-slate-50 p-3"><p className="font-mono text-xs font-bold uppercase text-rose-700">{item.risk_level}</p><p className="mt-2 text-sm font-black text-slate-900">{item.area}</p><p className="mt-1 text-xs text-slate-600">Owner/status: {item.responsible_role} / {item.status}</p><p className="mt-1 text-xs text-rose-700">Blockers: {item.blockers.join(', ')}</p></div>)}</div></section> }
