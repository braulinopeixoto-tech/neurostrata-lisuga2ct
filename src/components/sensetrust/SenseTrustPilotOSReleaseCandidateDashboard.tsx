import type { SenseTrustPilotOSReleaseCandidate } from '@/types/sensetrust/pilot-os-release-candidate'
const badges = ['simulated_only', 'metadata_only', 'human_review_required', 'release_candidate_only', 'demo_preparation_only', 'command_center_linked', 'no_real_operation', 'no_contract', 'no_client', 'no_diagnostic_certification']
export function SenseTrustPilotOSReleaseCandidateDashboard({ candidates }: { candidates: SenseTrustPilotOSReleaseCandidate[] }) { const rc = candidates[0]; return <section className="rounded-md border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xl font-black text-slate-950">SenseTrust Pilot OS Release Candidate v4.3</p><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">release candidate / acceptance criteria / demo readiness / institutional package</p></div><span className="rounded-md bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">no real operation</span></div><div className="mt-4 grid gap-3 sm:grid-cols-6"><Metric label="rc score" value={rc.releaseCandidateScore} /><Metric label="criteria" value={rc.acceptanceCriteria.length} /><Metric label="gates" value={rc.readinessGates.length} /><Metric label="flows" value={rc.demoFlows.length} /><Metric label="packages" value={rc.institutionalPackages.length} /><Metric label="routes" value={rc.navigationMap.routes.length} /></div><div className="mt-4 flex flex-wrap gap-2">{badges.map((x) => <span key={x} className="rounded bg-slate-100 px-2 py-1 text-xs font-bold uppercase text-slate-700">{x}</span>)}</div></section> }
function Metric({ label, value }: { label: string; value: number }) { return <div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-1 font-mono text-xl font-black text-slate-950">{value}</p></div> }
