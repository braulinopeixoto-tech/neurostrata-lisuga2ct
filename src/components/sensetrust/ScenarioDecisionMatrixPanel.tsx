import type { SenseTrustScenarioDecisionMatrix } from '@/types/sensetrust/strategic-scale-simulation-console'
export function ScenarioDecisionMatrixPanel({ matrices }: { matrices: SenseTrustScenarioDecisionMatrix[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Scenario Decision Matrix</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{matrices.slice(0, 8).map((item) => <div key={item.matrix_id} className="rounded-md bg-slate-50 p-3"><p className="font-black text-slate-900">{item.recommended_decision}</p><p className="text-xs text-slate-600">{item.decision_items.length} decisoes simuladas</p></div>)}</div></section> }
