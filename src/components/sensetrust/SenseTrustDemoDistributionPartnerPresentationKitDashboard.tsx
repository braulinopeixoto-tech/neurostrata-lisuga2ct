import type { SenseTrustDemoDistributionPartnerPresentationKit } from '@/types/sensetrust/demo-distribution-partner-presentation-kit'
const badges = ['simulated_only', 'metadata_only', 'human_review_required', 'demo_distribution_only', 'partner_presentation_only', 'release_candidate_linked', 'no_real_operation', 'no_real_email', 'no_real_crm', 'no_contract', 'no_client', 'no_diagnostic_certification']
export function SenseTrustDemoDistributionPartnerPresentationKitDashboard({ kits }: { kits: SenseTrustDemoDistributionPartnerPresentationKit[] }) { const kit = kits[0]; return <section className="rounded-md border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xl font-black text-slate-950">SenseTrust Demo Distribution / Partner Presentation Kit v4.4</p><p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">partner kit / demo distribution / claims matrix / meeting preparation</p></div><span className="rounded-md bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">no real email or CRM</span></div><div className="mt-4 grid gap-3 sm:grid-cols-6"><Metric label="demo" value={kit.demoDistributionReadinessScore} /><Metric label="claims" value={kit.claimsSafetyScore} /><Metric label="materials" value={kit.materials.length} /><Metric label="packages" value={kit.presentationPackages.length} /><Metric label="claims ok" value={kit.authorizedClaims.length} /><Metric label="claims no" value={kit.prohibitedClaims.length} /></div><div className="mt-4 flex flex-wrap gap-2">{badges.map((x) => <span key={x} className="rounded bg-slate-100 px-2 py-1 text-xs font-bold uppercase text-slate-700">{x}</span>)}</div></section> }
function Metric({ label, value }: { label: string; value: number }) { return <div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold uppercase text-slate-500">{label}</p><p className="mt-1 font-mono text-xl font-black text-slate-950">{value}</p></div> }
