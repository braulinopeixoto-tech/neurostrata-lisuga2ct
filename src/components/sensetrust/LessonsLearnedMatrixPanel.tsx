import type { SenseTrustLessonsLearnedMatrix } from '@/types/sensetrust/institutional-pilot-closeout'
export function LessonsLearnedMatrixPanel({ matrices }: { matrices: SenseTrustLessonsLearnedMatrix[] }) { return <section className="rounded-md border bg-white p-5 shadow-sm"><p className="text-lg font-black text-slate-950">Lessons learned matrix</p><div className="mt-4 grid gap-3 md:grid-cols-2">{matrices.map((item) => <div key={item.matrix_id} className="rounded-md bg-slate-50 p-3"><p className="text-sm font-black text-slate-900">{item.area} / {item.suggested_decision}</p><p className="mt-1 text-xs text-slate-600">Lessons: {item.lessons.join(', ')}</p><p className="mt-1 text-xs text-slate-600">{item.impact}</p><p className="mt-1 text-xs font-bold text-slate-700">{item.recommendation}</p></div>)}</div></section> }
