import type { CommandCenterReleaseCandidatePreparation } from '@/types/sensetrust/command-center-integration'
export function CommandCenterReleaseCandidatePreparationPanel({ preparations }: { preparations: CommandCenterReleaseCandidatePreparation[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="font-black">Release Candidate preparation</h2><div className="mt-2 grid gap-2 lg:grid-cols-3">{preparations.map((x) => <article key={x.id} className="rounded bg-slate-50 p-2 text-sm"><p className="font-black">{x.targetVersion}: {x.readinessScore}</p><p>{x.blockers.join(', ')}</p></article>)}</div></section> }
