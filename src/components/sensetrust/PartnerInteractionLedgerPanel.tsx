import type { SenseTrustPartnerInteractionLedger } from '@/types/sensetrust/strategic-partner-engagement-control-room'
export function PartnerInteractionLedgerPanel({ ledgers }: { ledgers: SenseTrustPartnerInteractionLedger[] }) { return <section className="rounded-md border bg-white p-4"><h2 className="text-base font-black text-slate-950">Partner Interaction Ledger</h2><div className="mt-3 grid gap-2 md:grid-cols-4">{ledgers.map((ledger) => <div key={ledger.interaction_ledger_id} className="rounded-md bg-slate-50 p-3"><p className="font-bold text-slate-900">{ledger.items.length} interacoes</p><p className="text-xs text-slate-600">sem CRM/e-mail real</p></div>)}</div></section> }
